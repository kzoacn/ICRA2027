\documentclass[letterpaper,10pt,conference]{ieeeconf}
\IEEEoverridecommandlockouts
\overrideIEEEmargins
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{cite}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\pdfminorversion=4
\pdfinfo{/Title (Route B: An Asset-Informed Geometric Baseline for Language-Conditioned Manipulation)
 /Author (Anonymous) /Subject (ICRA 2027 anonymous manuscript)}
\input{generated/numbers}
\newcommand{\method}{Route B}
\newcommand{\R}{\mathbb{R}}
\title{\LARGE\bf Route B: An Asset-Informed Geometric Baseline\\
for Language-Conditioned Manipulation}
\author{Anonymous Authors}

\begin{document}
\maketitle
\thispagestyle{empty}
\pagestyle{empty}

\begin{abstract}
Language-conditioned manipulation requires semantic grounding, geometric
reasoning, and reliable transitions between physical interactions. We study
how far an explicit, asset-informed system can address these requirements
without training an action policy on robot demonstrations.
\method{} combines a constrained language compiler, frozen visual grounding,
calibrated RGB-D reconstruction, and a sequential dispatcher for grasp--place
and contact skills. Its geometric state distinguishes observed surfaces from
offline object priors and maintains the displacement between the gripper and
the carried object during placement. Contact skills use robot measurements
and visual checks to manage articulation and recovery. The policy interface
excludes task-success feedback, while an external evaluator records whether
the official predicate is ever satisfied.
\ifRunComplete
On 400 fixed initializations spanning 40 LIBERO tasks, the deployed system
succeeds in \SuccessCount{} trials (\SuccessRate{}\%).
\else
In a preliminary snapshot of an ongoing 400-trial evaluation, it succeeds in
\SuccessCount{} of \EpisodeCount{} completed trials (\SuccessRate{}\%).
\fi
Controller completion and external success disagree in
\DisagreementCount{} trials, demonstrating why these outcomes must be
reported separately. The study characterizes a reproducible geometric
baseline with explicit asset and calibration assumptions and places its
measured outcomes alongside published action-policy results.
\end{abstract}

\section{Introduction}
Natural-language instructions provide a compact description of a manipulation
goal, but leave much of the physical procedure unspecified. To place a bowl
inside a drawer, a robot must identify the intended object, infer accessible
geometry, establish a stable grasp, maintain clearance during transport, and
release the object in a suitable region. Articulation introduces an additional
difficulty: the robot must distinguish productive motion from mechanical
resistance and decide when to release or reacquire contact. A plausible
semantic interpretation alone does not resolve these questions.

Large vision-language-action policies offer one route to this capability.
OpenVLA~\cite{openvla} adapts pretrained representations to action prediction,
while SmolVLA~\cite{smolvla} studies a more compact architecture and efficient
execution. These approaches motivate an accompanying question: what performance
can an explicit system achieve when visual grounding is pretrained but the
action policy is constructed from geometry and hand-engineered skills?
Answering this question requires disclosing the information available to the
system, including asset knowledge and calibration, rather than equating the
absence of policy training with the absence of prior information.

We present \method{} (Fig.~\ref{fig:architecture}), an asset-informed baseline for language-conditioned
tabletop manipulation. The system compiles supported instructions into
ordered skill programs and binds their entities to geometry reconstructed
from two calibrated RGB-D views. It uses a frozen detector together with a
gallery derived from public simulator assets. A dispatcher preserves state
across grasp--place segments and invokes specialized contact controllers for
drawers, doors, knobs, and pushing. Robot actions are bounded Cartesian pose
increments with a gripper command.

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{\input{figures/architecture}}
\caption{System organization. Language selects the ordered skill program;
calibrated observations and offline asset priors support geometric grounding.
The dispatcher invokes grasp--place or contact control and receives subsequent
observations. The external evaluator records task success without returning
it to the policy. The diagram summarizes software data flow, not a verified
process-security boundary.}
\label{fig:architecture}
\end{figure*}

The central engineering issue is continuity across physical stages. A
semantically correct target can still be unreachable with the current wrist
orientation; a successful close command need not establish retention; and a
gripper placed at a receptacle center need not center the object being carried.
\method{} therefore maintains explicit object-to-gripper displacement,
re-observes relevant geometry at selected transitions, and uses local
completion checks before advancing its program. These checks are fallible
estimates, separate from benchmark success.

We evaluate the deployed implementation on the Spatial, Object, Goal, and Long
suites of LIBERO~\cite{libero}. Each suite contains ten evaluated tasks, with
ten official initial states per task. The evaluation uses the existing
controller and fixed evaluation configuration; its scope is a system study on
known benchmark assets and instruction forms. We make three contributions:

\begin{enumerate}
\item An integrated language-to-geometry execution architecture that declares
      its offline priors and separates policy observations from external scoring.
\item A documented account of how carried-object geometry, visual checks, and
      contact-specific controllers are composed in the deployed system.
\item A record-based evaluation reporting task-level outcomes, execution costs,
      and disagreement between internal completion and external success.
\end{enumerate}

The contribution is the integrated baseline and its empirical characterization,
including the relationship between the controller's local decisions and the
benchmark's externally observed outcomes.

\section{Related Work}
\subsection{Language, Programs, and Geometric Planning}
Task and motion planning combines discrete decisions with continuous geometric
feasibility~\cite{tamp}. Language-conditioned systems extend this interface:
SayCan connects language choices to available skills and their affordances
\cite{saycan}, while Code as Policies generates executable programs that call
perception and control interfaces~\cite{codepolicies}. \method{} uses a
deterministic grammar and a fixed skill vocabulary. This makes the represented
program inspectable, but restricts language coverage to implemented
constructions and validated entities.

VoxPoser constructs spatial value maps to guide motion~\cite{voxposer}.
ReKep expresses manipulation objectives through relational keypoint
constraints~\cite{rekep}. These works show the utility of intermediate
geometric representations. Our implementation uses object and fixture
geometry, carried-object offsets, and specialized finite-state controllers.
It does not synthesize arbitrary constraint programs or perform a unified
optimization over the complete task. Its behavior depends on the coverage
and calibration of its manually implemented skills.

\subsection{Pretrained Perception and Action Policies}
Grounding DINO supports text-conditioned object detection~\cite{groundingdino}.
\method{} uses a frozen local checkpoint as one source of region proposals;
the downstream geometric and gallery components remain essential to the
implemented pipeline. In particular, public object textures and collision
dimensions provide priors that are stronger than an image-only input setting.

OpenVLA~\cite{openvla} and SmolVLA~\cite{smolvla} learn action generation from
robot data. OpenVLA-OFT~\cite{openvlaoft} studies action chunking, parallel
decoding, continuous action prediction, and additional sensor inputs.
\method{} provides a complementary, explicitly engineered control
baseline. The distinction concerns how actions are obtained; it does not imply
an absence of learning in the pretrained detector or an absence of development
on benchmark scenarios. Section~\ref{sec:comparison} compares reported
success rates while identifying differences in sensing, asset access, and
evaluation conditions.

\subsection{Benchmark Interpretation}
LIBERO was introduced to study knowledge transfer in lifelong robot
learning~\cite{libero}. We use its four commonly evaluated ten-task suites to
characterize a fixed manipulation system, rather than measuring continual
learning or forgetting. The choice of ten trials per task follows the sample
count used in SmolVLA's simulation study~\cite{smolvla}. Matching that count
alone does not reproduce its training or evaluation conditions. We specify
the remaining conditions in Section~\ref{sec:protocol}.



\section{System}
\subsection{Observation and Prior Information}
At step $t$, the policy receives
\begin{equation}
 o_t=\left(\{I_t^c,D_t^c,K^c,T_{WC,t}^c\}_{c\in\{a,w\}},r_t\right),
\end{equation}
where $a$ and $w$ denote the fixed and wrist cameras, $I$ is RGB,
$D$ is metric depth, $K$ is calibration, and $T_{WC}$ maps camera
coordinates into the world frame. Robot measurements $r_t$ include
end-effector pose, joint state, gripper state, and available force/torque
measurements. The grasp--place adapter uses pose, joint positions, and gripper
width; contact execution can access the broader robot measurement interface.

These are simulated, calibrated measurements. In particular, camera
extrinsics and proprioception are supplied by the simulator adapter, and
depth is not an estimate from monocular RGB. The policy observation types
contain no object pose labels, segmentation IDs, BDDL predicates, reward,
or task-success flag. Task identifiers and initial-state indices remain with
the evaluator; reset passes the instruction and a nonsemantic episode token.
This is a software interface property supported by code inspection, not a
claim of adversarial isolation.

Offline information is represented by a prior collection $\mathcal{P}$.
It includes object texture histograms, dimensions derived from public
collision geometry, robot geometry, class-specific handling rules, and
motion thresholds. The evaluated controller is therefore
$a_t=\pi(o_{\leq t},\ell;\mathcal{P})$, where $\ell$ is the instruction.
No action-policy fitting or demonstration loading occurs in this evaluation.
The detector's pretraining and the controller's manual development remain
part of the system's knowledge.

\subsection{Constrained Language Compilation}
The compiler maps supported language to
\begin{equation}
 \mathcal{G}(\ell)=(s_1,\ldots,s_M),\qquad
 s_j=(k_j,e_j^{\rm src},e_j^{\rm dst},\rho_j).
\end{equation}
Here $k_j$ is a skill, the entity bindings specify source and destination,
and $\rho_j$ represents a placement relation or instance selector.
The vocabulary contains pick, place-on, place-in, relative placement,
stack, open, close, push, turn-on, and turn-off.
For example, a two-object basket instruction becomes two successive
pick/place pairs. A drawer instruction can produce an articulation segment
followed by a manipulation segment.

Bindings retain relations such as \emph{next to}, \emph{between}, or an ordering
among visible instances. They do not encode the simulator's object identity.
The complete program undergoes capability checks before motion, including
valid skill arguments and consistency of held-object state. Compilation
success only establishes representational support; it does not establish
physical executability. The deployed grammar is tailored to known LIBERO
phrases and an allow-listed entity vocabulary. An optional semantic-resolver
interface exists, but no language-model resolver is configured in this run.

\subsection{Visual Grounding and Geometric State}
Each calibrated pixel $\tilde u=(u,v,1)^\top$ with valid depth contributes
\begin{equation}
 x_W=R_{WC}\big(D(u,v)K^{-1}\tilde u\big)+p_{WC}.
 \label{eq:backproject}
\end{equation}
Workspace filtering, support estimation, and connected components yield
candidate point sets. The pipeline fits observed bounds and uses
texture/size priors to classify and complete partial observations. The
gallery computes a color similarity
\begin{equation}
 b(h,h_k)=\sum_i\sqrt{h_i h_{k,i}}
\end{equation}
between normalized HSV histograms. Its score combines color and shape terms,
with configured weights $0.4$ and $0.6$, a softmax temperature of $0.13$,
and additional class-dependent footprint gates. Grounding DINO supplies
language-conditioned boxes that are converted to 3-D hypotheses using depth.
Thus a detector box is a proposal, rather than an oracle object mask.

Cross-camera hypotheses with matching labels are clustered in 3-D. For a
cluster, the implemented fusion weights are proportional to
$\max(q_i,0.05)\sqrt{n_i}$, where $q_i$ is confidence and $n_i$ is point
count. The default center-distance threshold is $4.5$\,cm. These thresholds
are empirical operating choices; nearby repeated objects can still produce
ambiguous associations.

The state retains both observed support/bounds and completed geometry.
This distinction matters when an object is partially visible: a size prior
can stabilize a center estimate, but should not be mistaken for measured
free space. Geometric selectors resolve the intended instance from the
available candidates and reference objects. At selected phase transitions,
new views are requested and checked against prior observations to reduce
hand-occlusion-induced jumps. Specialized fixture and cavity routines refine
support surfaces, opening regions, and admissible approach geometry.

\subsection{Grasping, Transport, and Placement}
Consecutive manipulation skills share a grasp--place controller. Its phases
include acquisition, pregrasp motion, approach, finger closure, retention
checking, lift, transport, descent, release, and retreat. Transitions combine
pose tolerances, measured gripper width, visual evidence, and bounded retry
logic. Different grasp modes account for exterior pinch, rim pinch, and
interior expansion; mode choice depends on the object and observed
accessibility. These are mechanical templates with specialized branches.

For target pose $(p^\star,R^\star)$, the basic servo emits
\begin{equation}
 a_t=\operatorname{clip}_{[-1,1]}
 \begin{bmatrix}
 (p^\star-p_t)/s_p\\
 \operatorname{Log}(R^\star R_t^\top)^\vee/s_R\\
 g_t
 \end{bmatrix}.
 \label{eq:servo}
\end{equation}
The general grasp--place defaults are $s_p=0.05$\,m and
$s_R=0.25$\,rad; cavity handling can select another rotation scale.
The nominal pose tolerances are $8$\,mm and $0.12$\,rad. These are
normalization scales and transition tolerances, not measured tracking-error
bounds or a continuous-time stability guarantee.

The gripper and carried object generally have different centers.
Let $\hat d_t$ be the estimated world-frame vector from the end effector to
the carried-object reference point. For desired object location
$p_o^\star$, placement planning uses the geometric relation
\begin{equation}
 p_{\rm ee}^\star=p_o^\star-\hat d^\star,\qquad
 \hat d^\star=\Delta R\,\hat d_t
 \label{eq:offset}
\end{equation}
when the planned wrist motion rotates the held geometry by $\Delta R$.
The implementation adds support, release-height, aperture, and
fixture-specific corrections to this relation. Offset estimates may come
from precontact geometry or refreshed RGB-D evidence; their origin is retained
in the execution trace. This allows inspection of placement decisions without
assuming that all offset estimates are equally reliable.

Release is followed by sensor-based verification. A local geometric check can
reject a placement even when the benchmark predicate has already been true,
or accept a relation that the benchmark does not recognize. Section
\ref{sec:outcomes} quantifies this distinction rather than using controller
completion as the reported success metric.

\subsection{Contact Skills and Sequential Execution}
The dispatcher separates consecutive grasp--place steps from atomic contact
skills. Drawer and door interactions use observed fixture geometry to
construct approach, engagement, motion, release, and retreat stages.
Knob and push skills use their own target detectors and motion templates.
Shared episode state includes an initial robot-pose reference and, where
applicable, an observed drawer anchor for subsequent placement.

Contact evidence combines position error, lack of measured progress, and
changes in end-effector force. One implemented guard requires a minimum
contact duration and bounded position error, together with either a stalled
error reduction or sufficient force change. Such a guard indicates a
candidate contact condition; it is not the task predicate. Specialized
post-contact visual checks assess the resulting fixture configuration.

Local recovery is bounded by phase counters and geometric checks.
Examples include releasing a rejected grasp, reacquiring a visible handle,
or performing a short clearance motion after stalled drawer interaction.
These routines preserve the distinction between a robot-side mechanical
signal and an estimate of task completion. Their hand-selected parameters
are part of the fixed controller configuration evaluated below.

\begin{table}[t]
\caption{Declared conditions for the evaluated system.}
\label{tab:conditions}
\centering
\footnotesize
\begin{tabular}{@{}p{.32\columnwidth}p{.63\columnwidth}@{}}
\toprule
Property & Setting \\
\midrule
Policy observations & Fixed and wrist RGB-D, camera calibration, robot measurements \\
Offline priors & Public asset textures/dimensions, robot geometry, hand-written skill rules \\
Action generation & Geometric state machines and clipped Cartesian increments \\
Learning during run & None; frozen Grounding DINO checkpoint \\
Scene truth in policy & No object pose labels, segmentation IDs, or BDDL predicates in observation types \\
Success feedback & External latch; no score-driven policy stopping \\
Coverage & Known benchmark assets and implemented instruction grammar \\
\bottomrule
\end{tabular}
\end{table}

\section{Experimental Protocol}
\label{sec:protocol}
\subsection{Tasks, Initial States, and Runtime}
The evaluation contains $4\times10\times10=400$ episodes:
Spatial, Object, Goal, and Long, ten tasks each, and official initial-state
indices $0$--$9$. Long is named \texttt{libero\_10} in the implementation.
Global random generators and the environment configuration use seed $7$.
The environment adapter preserves or replays the task reset stream when
starting at a later initial-state index, because resetting can affect fixture
configuration beyond the saved joint state.

We use two $256\times256$ RGB-D cameras and a Panda with operational-space
pose control. A ten-step settling period precedes scored actions. The
Spatial/Object/Goal/Long action budgets are $220/280/300/520$, matching
the numerical budgets in the OpenVLA evaluation script~\cite{openvlacode}.
Our environment seed and success-handling logic differ from that script;
its environment utility uses seed $0$.
The run uses Ubuntu 22.04, Python 3.12, PyTorch 2.11.0 with CUDA 12.8,
MuJoCo 3.3.2~\cite{mujoco}, robosuite 1.4.0~\cite{robosuite}, and
hf-libero 0.1.4 on one RTX 5090.

Task processes execute independently under a dispatcher, initially with four
workers and then eight. Each worker evaluates ten initializations of one
task. Ten Spatial task-00 episodes are reused from a stopped earlier run:
they match the fixed index list and the evaluation-affecting configuration,
and their source records were frozen before the new campaign. Their
inclusion was determined by identity and compatibility, not outcome.
The other 390 episodes are newly executed. We report this reuse explicitly
and keep timing analyses separate.

\subsection{External Scoring and Internal Completion}
Let $q_t$ be the official success predicate observed by the evaluator.
With policy-controlled stopping time $\tau$ capped at budget $H$, the
recorded outcome is
\begin{equation}
 Y=\bigvee_{t=1}^{\tau}q_t\ \lor\ q_{\rm final},
 \qquad \tau\leq H.
 \label{eq:score}
\end{equation}
The final query also handles a zero-action stop. Reward and success do not
enter the policy observation. The evaluator continues after a success signal
until the controller requests stopping or the action budget expires.
The score therefore represents \emph{ever reaching} the predicate during
this rollout, not necessarily satisfying it at the final frame.

Each record additionally stores terminal controller state, step count,
elapsed time, failure message, and geometric traces. The reported success rate
is $\widehat p=N^{-1}\sum_iY_i$. Equal task sample counts make the overall
episode average equal to the macro-average over tasks in the completed run.
We retain all planned failures; no outcome-based episode retry or selection
is used. Controller-internal retries remain part of a single episode.

\subsection{Scope of the Evidence}
\ifRunComplete
The completed campaign passes its record-coverage validation: 400 unique
episodes, fixed configuration, existing recorded videos, and matching
deployed source checksums.
\else
This draft contains \EpisodeCount{} completed episodes from the ongoing run.
Its figures and tables are a partial snapshot and must not be read as the
final 400-episode result.
\fi
Tables describing our measurements are generated from a frozen projection
of the original records, with hashes retaining the connection to their source
lines. Published comparison values are transcribed from the cited tables
and stored separately from our episode records.
Our rates summarize the specified initial states; per-task counts retain
the sample size and contribution of each task to the aggregate.

\begin{table}[t]
\caption{Observed success and execution cost. SR uses the external
ever-success predicate. $^\dagger$Elapsed-time medians use newly executed
episodes only; steps include reused records. Times include the recorded
episode execution overhead under concurrent evaluation.}
\label{tab:results}
\centering
\footnotesize
\input{generated/suite_table}
\end{table}

\section{Results and Analysis}
\subsection{Comparison with Published Policy Results}
\label{sec:comparison}
Table~\ref{tab:published} compares our measurements with published LIBERO
success rates, retaining the original papers' reported averages.
Our implementation is the only system evaluated in the present campaign.

The table specifies the distinct sensing and policy-training settings.
The OpenVLA study uses filtered demonstrations~\cite{openvla}.
SmolVLA trains across all 40 tasks and evaluates ten trials per task
\cite{smolvla}. The selected OpenVLA-OFT setting uses filtered demonstrations
and evaluates 500 trials per suite~\cite{openvlaoft}.
\method{} uses calibrated RGB-D, asset priors, and the scoring rule in
Eq.~\eqref{eq:score}. These differences make the table a comparison of
reported system outcomes under their stated conditions, rather than a
controlled ranking.

Our \SuccessRate{}\% overall rate is close numerically to SmolVLA's reported
87.3\%, while the strongest listed policy result is OpenVLA-OFT at 97.1\%.
Long is the lowest-scoring suite for every listed method, including
\method{}. This descriptive pattern motivates examining which multi-stage
tasks account for our remaining failures.

\begin{table*}[t]
\caption{LIBERO success rates (\%). Published rows contain the reported
means from OpenVLA Table~12~\cite{openvla}, SmolVLA Table~2~\cite{smolvla},
and OpenVLA-OFT Table~I~\cite{openvlaoft}; they are not reruns by us.
The OpenVLA-OFT row uses the full setting with additional inputs and filtered
demonstrations. The final row is measured in this work.
All methods receive language instructions; sensing, priors, training, and
evaluation protocols differ as described in the text.}
\label{tab:published}
\centering
\footnotesize
\input{generated/comparison_table}
\end{table*}

\subsection{Aggregate and Task-Level Performance}
\ifRunComplete
The system succeeds in \SuccessCount{} of 400 episodes, for an overall
success rate of \SuccessRate{}\% (Table~\ref{tab:results}).
\else
The current snapshot succeeds in \SuccessCount{} of \EpisodeCount{} completed
episodes (\SuccessRate{}\%); the remaining planned trials are pending.
\fi
Suite success rates are \SpatialRate{}\% for Spatial, \ObjectRate{}\% for
Object, \GoalRate{}\% for Goal, and \LongRate{}\% for Long.
The newly executed subset succeeds in \FreshSuccessCount{} of
\FreshCount{} trials (\FreshSuccessRate{}\%), allowing the effect of
including the ten reused records to be inspected directly.

Figure~\ref{fig:tasks} shows the task-level distribution.
\PerfectTaskCount{} tasks have ten successful recorded trials.
The hardest observed task is Long 09, placing a mug inside a microwave and
closing it, with $0/10$ successes. Its records contain four placement-target
localization failures, three placement-verification rejections, and three
phase timeouts. Long 08, placing both moka pots on the stove, succeeds in
$4/10$ trials. These cases expose limitations in multi-stage execution that
are hidden by the overall average.

Drawer-related tasks also remain difficult: Goal task 03, opening the top
drawer and placing a bowl inside, succeeds in $3/10$ trials; Spatial task 04,
retrieving a bowl from a top drawer, succeeds in $5/10$; and Long task 03,
placing a bowl in a bottom drawer and closing it, succeeds in $6/10$.
All five task groups were complete before analysis. The records identify
specific scenarios and stopping reasons for further diagnosis, but do not
show that articulation alone causes the failures.

The contrast between these tasks and simpler placement scenarios suggests
that a single aggregate score conceals important differences in grounding,
accessibility, and stage composition. It also cautions against interpreting
the suite name as a reliable ordering of control difficulty. The task-wise
counts expose these differences without changing the planned sample weights.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/per_task.pdf}
\caption{External successes divided by recorded trials for each official task
index. Columns identify different tasks in each suite; darker cells indicate
higher success. Counts, rather than rounded percentages alone, show the
sample size supporting each result.}
\label{fig:tasks}
\end{figure*}

\subsection{Controller Completion Is a Different Outcome}
\label{sec:outcomes}
Table~\ref{tab:status} crosses terminal controller state with external
success. In \ControllerFalsePositive{} episodes the controller reports
completion while the evaluator reports failure. Conversely,
\ControllerMissedSuccess{} episodes receive external success without
controller completion. The combined disagreement is
\DisagreementCount{}/\EpisodeCount{} (\DisagreementRate{}\%).
The latter group includes cases where a predicate is reached before a later
local failure or budget exhaustion. Because the recorder retains an
ever-success latch, these records do not imply successful final-state
verification.

Using local completion as the benchmark label would change the measured
outcome; feeding external success to the controller would change its
information interface. We record both values separately.
The records lack first-success time and a separate final-only label, so they
cannot quantify changes in final-state success from continued execution
or the runtime saved by an early-stop evaluator.

\begin{table}[t]
\caption{Terminal controller state and independently recorded external
ever-success. A controller failure can follow an earlier benchmark success.}
\label{tab:status}
\centering
\footnotesize
\input{generated/status_table}
\end{table}

\subsection{Recorded Failure Categories}
Table~\ref{tab:failures} groups the \FailureCount{} externally unsuccessful
episodes using their persisted status and failure message.
The grouping gives controller completion/external failure and global-budget
exhaustion precedence; remaining messages are classified by phase timeout,
grasp/retention rejection, sensor-verification rejection, or another failure.
This is a reproducible description of the recorded stopping reason, not a
manually verified taxonomy of physical root causes.

A phase timeout may arise from an inaccessible target, calibration error,
occlusion, or contact resistance. Likewise, a rejected sensor verification
may reflect an actual placement error or perception uncertainty.
Determining these causes requires frame-level or geometric-trace analysis.
The current evidence supports identifying where execution terminates, while
leaving causal attribution open.

\begin{table}[t]
\caption{Outcome groups among external failures. Categories are derived from
recorded controller messages with a fixed precedence rule.}
\label{tab:failures}
\centering
\scriptsize
\input{generated/failure_table}
\end{table}

\subsection{Execution Cost and Qualitative Context}
The median newly executed episode takes \FreshMedianSeconds{} seconds.
Mean action counts range across suites as shown in Table~\ref{tab:results}.
Long episodes average \LongMeanSteps{} actions, compared with
\ObjectMeanSteps{} for Object. These costs include controller execution
through its own stopping condition; they are not time-to-first-success.
Recorded elapsed times mix perception, simulation, rendering, and video
overhead while workers compete for the same CPU and GPU.
They should not be interpreted as real-robot control frequency.

The run reports \RuntimeExceptionCount{} runtime exceptions.
\ifRunComplete
Dispatcher wall time is \RunMinutes{} minutes for the campaign containing
390 new and ten reused episodes.
\else
Dispatcher wall time at the snapshot is \RunMinutes{} minutes.
\fi
Figure~\ref{fig:rollouts} provides visual context from actual recordings.
The upper row shows a successful Object task-00 episode. The lower row is
the earliest externally unsuccessful Goal task-03 episode in the official
index order, which exhausts its action budget. Selection is stated to make
the figure interpretable as illustration, rather than a representative
sampling estimate or evidence of a particular failure mechanism.

\begin{figure*}[t]
\centering
\includegraphics[width=.90\textwidth]{figures/rollouts.pdf}
\caption{Actual fixed-camera frames from two recorded episodes. Top:
\emph{pick up the alphabet soup and place it in the basket} (Object 00,
initial state 00). Bottom: \emph{open the top drawer and put the bowl inside}
(Goal 03, initial state 02). Four uniformly spaced recorder indices are shown;
recording uses a stride of two actions. Images are crops of the fixed-camera
half of the original dual-view video, with no synthetic image content.}
\label{fig:rollouts}
\end{figure*}

\section{Discussion and Limitations}
Explicit geometry and specialized skills solve a substantial fraction of the
selected episodes under declared asset and calibration assumptions.
Texture and size priors encode known objects; grammar rules encode known
language forms; and specialized controllers reflect manual development.
These resources matter when interpreting the published-policy comparison.

\paragraph{Generalization and novelty.}
The run covers known assets, language forms, and one simulated embodiment
with calibrated depth. It does not evaluate transfer to unseen assets,
language, perturbed calibration, or physical hardware, and provides no
held-out history of controller development.
The rates characterize the deployed benchmark system.
Its geometric operations and state machines are established techniques;
the contribution is their documented integration and empirical
characterization.

\paragraph{Reliability and interpretation.}
Bounded actions and local guards limit the implemented commands, but do not
establish collision safety, contact-force safety, or real-world reliability.
The narrow ten-trial task samples can also conceal rare failure modes.
The observed controller/evaluator disagreement points to a specific
measurement improvement: record first-success time and final predicate
separately, then evaluate alternative stopping conventions without silently
changing the policy's observations.

\paragraph{Reproducibility.}
Source and resource checksums, fixed initial-state identifiers, and
record-derived figures support reconstruction of this evaluation.
The software-boundary counters are implementation-generated records, rather
than independent security attestations. The current artifact should therefore
be used to reproduce and inspect a concrete system, with the experimental
scope stated above.

\section{Conclusion}
We presented \method{}, an asset-informed geometric manipulation baseline
that compiles supported language into closed-loop grasp--place and contact
skills. Under its declared sensing and prior assumptions, the evaluated
system achieves \SuccessRate{}\% external success across
\EpisodeCount{} recorded episodes.
Task-level outcomes and completion disagreements reveal limitations that an
aggregate success rate alone would hide. The implementation and record-based
analysis make the system's information requirements, contact behavior, and
policy-evaluation interface available for inspection and reproduction.

\section*{Acknowledgments and AI Disclosure}
OpenAI Codex assisted in drafting and editing the text in all sections,
preparing the LaTeX source, and writing scripts for the numerical tables and
figures. Results for \method{} are computed from existing simulator evaluation
records; comparison values are taken from the cited publications.
The rollout images are extracted from our recordings; no
generative image model was used.

\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}
